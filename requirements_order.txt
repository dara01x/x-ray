# X-ray AI - Installation Order Reference
# This file shows the correct installation order to avoid dependency conflicts
# DO NOT use pip install -r on this file - use the install_xray_ai.py script instead

# STEP 1: Core web framework
# pip install Flask>=2.0.0,<3.0.0 Flask-CORS>=3.0.0,<5.0.0 Werkzeug>=2.0.0,<3.0.0

# STEP 2: Basic numerical processing  
# pip install numpy>=1.21.0 setuptools>=60.0.0

# STEP 3: PyTorch (CPU version recommended for compatibility)
# python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# STEP 4: Data processing
# pip install pandas>=1.3.0 scikit-learn>=1.0.0 scipy>=1.7.0

# STEP 5: Medical AI libraries (may need special handling)
# pip install torchxrayvision>=1.0.0

# STEP 6: Image processing
# pip install opencv-python>=4.5.0 Pillow>=8.0.0 albumentations>=1.3.0 scikit-image>=0.19.0

# STEP 7: Utilities
# pip install PyYAML>=6.0 requests>=2.25.0 tqdm>=4.60.0 typing-extensions>=3.10.0

# STEP 8: Visualization
# pip install matplotlib>=3.5.0 seaborn>=0.11.0

# STEP 9: Medical imaging
# pip install pydicom>=2.0.0

# STEP 10: Testing (optional)
# pip install pytest>=7.0.0 pytest-cov>=4.0.0

# TROUBLESHOOTING NOTES:
# - If PyTorch installation fails, use CPU version
# - If torchxrayvision fails, install dependencies first: pip install scikit-image tqdm
# - If albumentations fails, try: pip install --no-deps albumentations
# - For Python 3.13+, some packages may not have wheels available
# - On Windows, enable long path support or use short path virtual environment